// Store configuration variables here
export const config = {
  // You'll replace this with your actual API key, but NEVER commit this file with the real key
  anthropicApiKey: process.env.ANTHROPIC_API_KEY || "YOUR_ANTHROPIC_API_KEY"
};